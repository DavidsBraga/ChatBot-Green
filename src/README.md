# Project Name

## Description

#TODO
A brief description of your project goes here.

## Environment Setup

To set up your environment, you'll need to create a `.env` file in the root directory of your project with the following content:

```dotenv
python -m venv env_name

env_name\Scripts\activate
```

Replace `your-deployment-name` and `your-openai-key` with the actual values.

## Installation

Clone the repo:

Navigate to the project directory:

```bash
cd hackaton-genai
```


Create and activate a virtual environment
```bash
python -m venv venv
source venv/bin/activate  # On Windows use `.\venv\Scripts\activate`
```

Install dependencies:

```bash
pip install -r requirements.txt
```

## Usage 

Provide instructions on how to use the application, including any commands or scripts that need to be run.
```bash
python your_script.py
```